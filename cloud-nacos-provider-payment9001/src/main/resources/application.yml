server:
  port: 9001

spring:
  application:
    name: nacos-payment-service

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #nacos注册中心地址

management:
  endpoints:
    web:
      exposure:
        include: "*" #暴露所有监控端点
